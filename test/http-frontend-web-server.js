var manager = require('../lib/manager');
var fs = require('fs');
var a = (new manager)
a.frontEndServer(9999, function(socket) {
	
	console.log('frontEndServer start')
	
})

		
// console.log(a)
